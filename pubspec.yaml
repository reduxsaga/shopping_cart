name: shopping_cart
description: Shopping Cart Example by Redux Saga
version: 1.0.0+1
maintainer: Bilal Uslu (@bilaluslu)
homepage: https://github.com/reduxsaga/shopping_cart
repository: https://github.com/reduxsaga/shopping_cart

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  redux: ^5.0.0
  flutter_redux: ^0.8.2
  redux_saga: ^3.0.2
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.16.5

flutter:
  uses-material-design: true
